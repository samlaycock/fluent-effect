---
"fluent-effect": minor
---

Add `runOrThrow`, `runResult`, and sync/app variants for converting typed task failures into normal JavaScript boundary behavior without manually unwrapping Effect `Either` values.
