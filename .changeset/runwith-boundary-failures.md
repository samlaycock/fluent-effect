---
"fluent-effect": minor
---

Add `fx.runWithOrThrow` and `fx.runWithResult` one-shot dependency runtime helpers that unwrap dependency layer acquisition failures consistently with `fx.app` boundary helpers.
