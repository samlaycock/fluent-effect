---
"fluent-effect": patch
---

Treat `provideDependency` implementations as plain values so services can intentionally use Effects as dependency values. Use `provideDependencyTask` for task-built implementations.
